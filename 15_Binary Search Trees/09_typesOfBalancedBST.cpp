// we have seen that search, insert, delete function in BST, all have T:O(height)
// so, if height is logn , then T:O(logn) and if
// height is N , then T:O(N)
// so, to maintain logn height, we need to make balanced BST
// Balanced BST: for every node
// | height of node's left subTree - height of node's right subTree| <= 1.
// types of BST
// --AVL Tree
// --Reb Black Tree
// -- 2-4 Tree
// we will learn and implement them later.