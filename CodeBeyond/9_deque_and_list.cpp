#include <bits/stdc++.h>

using namespace std;

int main()
{

    // deque is exactly similar to vector
    // but it is bidirectional
    deque<int> d;

    // push_front(),push_back()
    // pop_front(), pop_back()
    // begin, rbegin, end,rend,
    // size,clear, empty, at()

    list<int> ls;

    // push_front(),push_back()
    // pop_front(), pop_back()
    // begin, rbegin, end,rend,
    // size,clear, empty, at()
    // remove();

    


    return 0;
}